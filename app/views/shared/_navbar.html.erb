<nav class="p-2">
  <div class="container mx-auto flex justify-between items-center">
    <div></div>
    <div class="flex">
      <% if user_signed_in? %>
        <%= button_to t(".sign_out"), destroy_user_session_path, method: :delete, class: "btn btn-light-red" %>
      <% else %>
        <div class="flex gap-2">
          <%= link_to t(".log_in"), new_user_session_path, class: 'btn btn-gray' %>
          <%= link_to t(".sign_up"), new_user_registration_path, class: 'btn btn-primary' %>
        </div>
      <% end %>
    </div>
  </div>
</nav>
